# Architecture — WiNet Lab

Status: Home, Research, Publications and Contact are implemented and included in the user-authorized 2026-09-18 release. Research has six papers, three each under the first two directions; the third direction and People retain placeholders. Deployment status is recorded in verification/release-2026-09-18.md; technical publication is separate from user acceptance.

## Boundaries

Local media optimization (2026-09-18, not yet published): Home imports the configured original from public/images into Astro's image pipeline and generates six responsive lossless WebP sizes with the original PNG fallback. ResearchPaperCard imports one shared research-video.ts controller: defer src until within 200px, play muted/loop only when visible, pause offscreen/hidden, preserve manual pause, and provide a noscript native-player fallback. Three previews have independent H.264/faststart compressed copies; all five have front-loaded indexes. Metasurface's original is now demo.mp4; Mighty remains explicitly shared because its original is already efficient. This supersedes earlier statements that Research previews need no client JS. See verification/media-optimization-2026-09-18.md.

Latest card behavior (2026-09-18): ResearchDirection stably sorts a copy of each papers array by full-demo availability, preserving order within each group. ResearchPaperCard omits missing Demo controls and renders disabled Paper coming soon for missing/withheld PDFs; the latter remain absent from public/build outputs. This replaces only the previous hidden Paper control UI, not the access restriction. Optional structured publication metadata (venue, year, published/submitted status) renders a warm brown serif row above the title when a venue exists. At the user's request, forthcoming papers keep venue/year unset; published papers use existing verified records. No client script or CMS added.

The Vite watcher excludes `**/resources/**` as well as `**/.tools/**`. Raw media imports are explicit, and watching source-video copies raised Windows EBUSY during the PDF access change. Website source/public watching remains active; resources are neither an automatic content source nor a deployment directory.

Withheld-paper policy: `pdf: null` renders a disabled Paper coming soon with a submission-process tooltip. Omitted PDFs also render a disabled placeholder. The underwater, EEG and cardiac PDFs are absent from public/build outputs; originals remain in Git-ignored resources. The download regression checks old URLs return 404 and eight permitted downloads preserve their bytes. The release uses a clean snapshot on the existing remote history; earlier local development history containing withdrawn PDFs stays only on a local archive branch, which must not be pushed.

Research download labels are Paper and Demo, with distinct inline document/play SVGs in the shared card (including disabled states). The user requested no visible media description on current or future cards, so figcaption and the video description schema field have been removed; video naming still uses the paper title, and optional caption tracks remain supported. Resource data keys remain pdf/demo.

Research card layout update (2026-09-18): `ResearchDirection` owns a two-column grid above 760px and a one-column grid on smaller screens. `ResearchPaperCard` stacks media/text vertically and uses 30px-high download controls. Videos use native muted autoplay, loop and inline playback, keeping controls for pausing; there is no additional playback script. Existing component checks verify autoplay before any user interaction and observe a real loop, alongside two-column wrapping and mobile stacking.

- `src/layouts/SiteLayout.astro`: document language, metadata, shared header/footer, global stylesheet, and skip link.
- `src/components/`: shared Brand, navigation, footer, section heading, NewsList, EmphasisText, and small decorative brand mark. Header owns its mobile-menu script. Brand reads the name from site.json for both header and footer. Pages do not import other pages.
- Mobile header layout uses a two-column grid with a minimum 86 px first row for the brand and menu button; the expanded navigation spans the next row. This keeps the toggle stationary and its focus outline visible. Regression tests use fixed click coordinates at 320, 390, and 760 px so they cannot silently follow a moving button.
- `src/components/Mark.astro`: single inline SVG source for the minimal W waveform and signal dot, with no external assets or IDs. `Brand.astro` splits the canonical site name into stacked words, paired with the same mark in the header and footer. It has no tagline prop. Wordmark colors and sizes live in the shared stylesheet; footer centers the component at a larger size. The SVG is decorative and the home link has the complete accessible name. Plain text color works in forced-colors mode without a gradient override.
- `src/pages/index.astro`: Home composition and build-time responsive image generation from the configured original.
- `src/pages/[section].astro`: generates the unfinished People route shell, excluding Research, Publications, and Contact.
- `src/pages/research.astro`: three-direction anchor index and numbered sections, sharing SiteLayout. `src/data/research.json` supplies stable IDs, titles, subtitles and per-direction papers, validated by `src/lib/research.ts`. ResearchDirection renders Demo-first cards or an explicit empty placeholder. Cards support native image/video previews, summaries, publication metadata, optional citations and independent same-origin PDF/full-demo downloads. Missing PDFs have disabled labels; missing Demos have no control. Previews never become implicit full-demo downloads. File URLs use the shared base helper, including encoded filename segments. Fragment targets accept keyboard focus. Six real papers follow the user's source-folder classifications. Shared research-video.ts controls preview loading and playback; Cite has its own independent dialog script.
- `public/research/{paper-id}/`: three currently open PDFs, five video previews/posters, one original image, two full-demo ZIPs (EEG: three originals; cardiac: four), and original single-video downloads. Metasurface uses an independent compressed preview and original demo.mp4; Mighty explicitly shares its unmodified display/demo copy; magnetic/RF uses a compatible H.264/AAC web preview and a separate original HEVC download. Git-ignored `resources/` remain untouched; new source folders are not automatically imported. The resources manifest records historical import hashes, not current PDF access authorization.
- `src/pages/contact.astro`: static Contact introduction, Join Us recruitment paragraph and mailto link, sharing SiteLayout and reading `src/data/contact.json`. Page-scoped responsive styles; no contact form, new client JavaScript, or external resources.
- `src/pages/publications.astro`: independent year-grouped bibliography and progressively enhanced search/year/type filters. Filters operate on escaped static DOM text with no external requests; disabled JavaScript leaves every citation visible. Mobile filters use a keyboard-accessible collapsed panel.
- `src/components/PublicationCard.astro`: citation rendering, author emphasis, resource links, and colored serif venue labels. The user requested removal of pending-details labels; verification flags remain internal audit metadata and do not affect rendering. `src/lib/publications.ts` defines types, build-time validation, stable ordering, and citation formatting. `src/data/publications.json` holds 74 retained records separately from presentation, including source numbers, original citations, verification flags, and available online dates; p71 was deleted at the user's request, and other IDs were not renumbered. Year groups in `src/pages/publications.astro` have prominent serif headings, tinted bands, rules, and spacing.
- `src/data/site.json`: identity, description, navigation. `src/data/home.json`: hero copy, image metadata, the three supplied introduction paragraphs, and seven supplied News records. Fixed interface labels remain in components.
- `src/lib/paths.ts`: shared base-path handling for internal page links and static image URLs. File URLs do not receive page-style trailing slashes.
- `public/images/winet-lab-wordcloud.png`: the user-supplied original Home image, 2154×1614; preserved without cropping or redrawing.
- `src/styles/global.css`: design tokens, layout, components, responsive rules. No external fonts or image services.
- `tests/` and `scripts/`: production-browser verification, screenshot capture, and a separate repository-base-path check.
- `scripts/check-research-card.mjs`: injects `tests/fixtures/research-card.astro` into isolated ignored builds only. It generates synthetic PDF/video/caption assets in those build outputs and verifies native playback, exact download bytes, optional-resource behavior, responsive layout and accessibility at `/` and `/WiNet-Lab/`. The fixture route/media are absent from regular production builds.

Astro generates five static HTML pages. Mobile navigation, Publications filters, Research preview playback and the Cite dialog use client JavaScript. `src/data/citations/*.{txt,bib,ris}` supplies reviewed reference exports, linked through Research papers' optional `citationId` and publisher metadata in sources.json. `lib/citations.ts` supplies identical display/copy/download text per format; the three fixed-extension routes under `src/pages/research/citations/` share `lib/citation-routes.ts` and export only referenced published entries as nine static files. Fixed extensions keep dev and build file URLs consistent under trailingSlash rules. CitationDialog uses a native dialog, radio format selection, keyboard focus handling and clipboard fallback; without JavaScript all three formats remain downloadable. The schema rejects citations on submitted papers. No application server, database, or remote CMS is configured. The user separately authorized publishing the subsequent Cite feature on 2026-09-18; see [its release record](verification/release-cite-2026-09-18.md). deploy.yml builds main with the /WiNet-Lab/ base and publishes dist through GitHub Pages.

NewsList receives structured records with unique id, YYYY-MM date, and nonempty text; validates those fields at build time; and sorts a copy by descending month while preserving input order within a month. It renders semantic list items and time elements, with a small empty state only when the list is empty. EmphasisText is shared by the hero description, introduction paragraphs, and News text. It supports paired **bold** markers only and renders semantic strong elements; other content remains escaped text. It does not render arbitrary HTML or provide a complete Markdown engine.

## Visual decisions

On 2026-09-18 the live MARS Lab Research page was successfully read and visually inspected. Its numbered direction index and section/image-text layout informed the WiNet framework; directions and the six imported papers/media come from the user. The third direction remains pending. This page-specific inspection does not imply pixel equivalence or change the historical Home reference-check limitation below.

The MARS Lab reference informed the five-page navigation. Following user review, Home now contains the visual introduction, group introduction, News, and footer; Events and Collaborators were removed from markup, content data, and styles. The reference is guidance, not a requirement to reproduce every section. Its page text and stylesheet structure were read; the live reference screenshot failed with ERR_CONNECTION_RESET. Pixel-level equivalence has not been verified or claimed.

This site uses a pure-white (#ffffff) page background, forest green accents, system sans-serif body text, and Georgia headings. The footer, News panel, and image container also use white. The supplied word cloud replaces the original network illustration; its original colors and proportions are preserved. The old NetworkArt component and caption were removed. Following the user's cancellation of image generation, the latest shared logo is a minimal green W waveform with an orange signal dot and a dark stacked wordmark. The user-supplied MARS Lab logo screenshot informed simplicity and icon/type proportions; its planet, orbit and star were not copied. This proposal is implemented but awaiting design acceptance. The footer slogan and contact CTA remain removed. No reference-site people, publications, partner logos, or photos were copied.

## Runtime

Exact versions are locked in package.json and package-lock.json. Portable Node.js v24.21.0 was downloaded from the official distribution and verified against its official SHA-256 manifest. It lives only in ignored .tools/. No global runtime or PowerShell execution-policy changes were made. A PowerShell helper was removed after script policy prevented its execution; the user launcher is a .cmd file.

Vite's development watcher excludes `**/.tools/**`. During the EEG import, copying large MP4 assets into `.tools/base-dist` triggered Windows EBUSY watcher errors and an Astro error overlay. Excluding the test/tool directory fixes this without disabling source/public-asset watching; a repeat base-path build and the actual dev preview were checked. Media inspection/poster extraction used imageio-ffmpeg 0.6.0 installed only into ignored `.tools/media-python`; it is not a site dependency or needed for deployment.

Astro requirements were checked against [official setup documentation](https://docs.astro.build/en/install-and-setup/) and registry metadata on 2026-09-15. The current CLI is node_modules/astro/bin/astro.mjs. Astro 7 may automatically detach CLI previews in agent environments; automated tests use its preview() API with a foreground process.

## Deployment boundary

The user supplied zjugxz/WiNet-Lab as the target on 2026-09-17. Astro site is https://zjugxz.github.io; SITE_BASE defaults to / for local previews. The .github/workflows/deploy.yml workflow supplies /WiNet-Lab/, uses Node 24 and the official Astro action to install dependencies and run type checking plus a static build, then deploys the generated artifact to Pages. It triggers on pushes to main and manual dispatch, with required Pages permissions and serialized deployments. Initial deployment and online Home verification passed after the user's manual push; later local commits do not imply authorization to publish again.

scripts/check-base.mjs defaults to /WiNet-Lab/ and supports SITE_BASE. It exercises five routes, CSS, the original word cloud and the mobile menu from an isolated .tools/base-dist build on port4323. The earlier /winet-group/ path was only a historical trial name. See [deployment instructions](github-pages-preview.md) for preparation, validation and remaining remote steps.

Preview metadata still includes noindex, nofollow; review it for a later public release. Origin is configured for zjugxz/WiNet-Lab. Local proof does not establish mainland-China or international production availability.

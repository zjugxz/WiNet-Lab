# Content maintenance and planned editing tool

Updated 2026-09-22. The interface is primarily English; the user allowed Chinese text on the site on 2026-09-22 (e.g. the NSFC news note （原优青） and the two supplied Chinese publication citations retain their original language). The online content editor is required but not implemented.

The user explicitly declined the proposed local content-management tool. The current priority is integrating real material into the initial five-page website; the final online editor follows. Do not build a local editor as an intermediate deliverable. Keep content separate from presentation so the final editor can use the same data. Home material, 74 retained publication records (75 originally supplied), Contact copy, and six Research papers across the first two directions are integrated. The third Research direction and People records are still pending.

## Editing at this milestone

Home media optimization (2026-09-18, local only): keep the original under public/images/ and heroImage.src/width/height/alt in home.json. index.astro imports the configured PNG/JPG/JPEG original through a glob and Astro generates responsive WebP sizes at dev/build time; the image-service config uses lossless WebP. The picture element retains the original PNG fallback and high priority. No manual WebP export is needed. Update sizes if the hero layout changes, and verify small words at desktop/mobile pixel ratios after changing the source. Generated production files live in dist/_astro and are not committed.

src/data/home.json contains hero copy, heroImage metadata, about.paragraphs (three user-supplied paragraphs rendered individually), and news.items. Each news record contains a unique id, date in YYYY-MM format, and text. Records display in descending month order, retaining input order within the same month. The headline accepts a newline for intentional line breaks. heroImage.src is relative to public/; width/height record original image dimensions and alt describes it in English. The current source is public/images/winet-lab-wordcloud.png. Events and Collaborators were removed at the user's request; do not restore those fields or sections without a new request. src/data/site.json contains the canonical name WiNet Lab, description, and navigation.

After a developer edits data, the development preview normally updates automatically; confirm the actual preview and restart it if stale (see the note below). Production output must be regenerated with npm run build. News, Research, Publications, and Contact render separate structured data sources; People remains a placeholder route. JSON files are not a user-facing form tool and do not replace the planned online editor.

## Research maintenance

Media optimization (2026-09-18, local only): research-video.ts is the shared controller used by ResearchPaperCard. Keep new previews in the same component to inherit near-viewport loading (200px), visible muted looping, offscreen/hidden-page pause, remembered manual pause and native no-JS controls. src is assigned only on demand; preload alone is not a lazy-loading mechanism. Browser autoplay restrictions retain manual playback. Test both root and repository bases with scripts/check-media-performance.mjs after builds. The current media/download checksum baseline is verification/media-optimization-manifest.json; update it deliberately after an authorized asset change, preserving original downloads.

For future conversions, run `node scripts/encode-preview.mjs INPUT OUTPUT FFMPEG_PATH` with a distinct, nonexistent output path. The script uses H.264 CRF23/slow, max1280×720 without enlargement, yuv420p, AAC stream copy and faststart. It requires an AAC-compatible input audio stream; inspect other inputs before conversion. Review text/frames, duration and actual playback before replacing the public preview. The script is an offline developer utility, not a CMS or an automatic import of resources. Keep downloadable originals separate whenever a shared preview needs compression. See [implementation and validation](verification/media-optimization-2026-09-18.md).

Cite (separately authorized for publication on 2026-09-18; extended to mmExpert on 2026-09-22): published cards set `"citationId": "paper-id"` (replaces the old bibtex field), referring to `src/data/citations/paper-id.txt`, `.bib` and `.ris`, with publisher URL/Text style in `sources.json`. Verify complete author order, journal, DOI, formal year and known volume/issue/pages against publisher records; omit unknown fields. Update all three exports together. Text currently uses Cell/IEEE styles retrieved through DOI content negotiation (conference papers use the IEEE conference form with the full proceedings name; their RIS uses TY CONF without volume/issue); BibTeX preserves unique keys and acronym braces; RIS retains full authors and separate start/end pages. The schema rejects citations on submitted papers and missing format files fail builds. CitationDialog defaults to Text, supports format switching and copies/downloads the active format; failed clipboard access selects text for manual copying. Without JS, Cite downloads Text with extra BibTeX/RIS links. Existing .bib URLs remain valid. No citation service runs in visitors' browsers. Run `node scripts/check-research-citations.mjs` after both root/base builds. This does not authorize restoring a withheld PDF. See [sources and verification](verification/research-cite-2026-09-18.md) and [release evidence](verification/release-cite-2026-09-18.md).

Current card behavior (latest 2026-09-18 instruction): cards with a full `demo` appear first within each direction; ordering is stable within each group. The source array remains unchanged. An absent Demo has no control at all. Both missing PDFs and explicitly withheld `pdf: null` show a disabled `Paper coming soon`; null also supplies the submission-process tooltip and must never leave a public PDF file. This supersedes the earlier hidden-control policy recorded below.

Optional `publication` metadata contains `venue` (nonempty text), `year` (integer), and required `status` (`published` or `submitted`) when the object exists. Example after acceptance: `"publication": { "venue": "Confirmed venue name", "year": 2027, "status": "published" }`. The year here is an example, not research data. The user explicitly asked to leave all forthcoming papers' venue/year unfilled for now; their objects contain only `"status": "submitted"`, and the metadata row is absent. Do not populate from guessed targets, filenames or older publication-list assumptions. Venue/year are displayed independently of PDF access; changing metadata does not automatically re-enable a download. When authorized to open the PDF, copy the approved version into public, replace null with its download object, and update the restricted-ID check.

The venue row is above the paper title: 18px bold italic Georgia, warm brown #a45122, with a smaller serif year. Fields are maintained in research.json; style is shared in ResearchPaperCard. This is a content-schema interface for later editing, not an implemented CMS or network API. Run both existing research verification scripts after changes; the access check also verifies stable sorting, no missing Demo controls, forthcoming metadata omission, real computed venue styles and four viewport widths.

During the PDF-withholding task, two Acoustic-Electromagnetic display/demo videos were added to resources; they remain source material, with no site card or PDF link yet. This supersedes the initial "not found in supplied folders" observation below. Vite now excludes `**/resources/**` as well as `.tools` from watching: source-media imports are explicit, and large copies in resources can otherwise raise Windows EBUSY and disrupt the preview. Application `src/` and `public/` watching remains enabled.

PDF access policy: `pdf: null` explicitly withholds the file and displays disabled Paper coming soon. Underwater visible-light backscatter, EEG fatigue/interaction and cardiac monitoring are currently withheld. Originals remain in Git-ignored resources. The fourth restricted title, Acoustic-Electromagnetic Information Sensing and Security Protection via Intelligent Transparency Metasurfaces, has source demo/display videos but no site card or PDF. Do not restore withheld files from the historical import manifest or infer permission from new source materials. Run `node scripts/check-research-downloads.mjs` after root/base builds to check states, missing files, HTTP 404 and remaining downloads. Change its restricted-ID list only with a user-approved policy change.

Hiding controls does not protect a file left in public: remove its site copy and rebuild. The 2026-09-18 release uses the current tree with the existing remote main as its parent, excluding earlier local commits containing withdrawn PDFs. The original history stays in local-only `codex/local-history-2026-09-18`; do not push it or use --all/--mirror. This preserves the developer's backup without exposing it in the published branch. Files already downloaded elsewhere cannot be revoked; no such external copy is claimed removed.

The restructured source folders are `resources/{paper,display,demo}/{direction}/{paper or paper folder}`. Six records now have website copies under `public/research/{stable-paper-id}/`. Map files explicitly: titles can differ from filenames, especially the magnetic/RF paper and the cardiac manuscript. Use the PDF's actual title and abstract; do not infer publication status from a manuscript filename. See the [complete source mapping](verification/research-resources-2026-09-18.md) and its JSON hash manifest.

Cardiac monitoring uses display/3.mp4 and a ZIP containing all four full-demo videos. The underwater paper has a public image, no full demo, and a withheld PDF. Metasurface originally shared byte-identical display/demo sources; its full-quality original now lives at demo.mp4 while preview.mp4 is compressed. Mighty remains explicitly shared and unmodified. Magnetic/RF has a separate H.264/AAC preview converted from HEVC; its Demo remains the original HEVC MP4. Preserve original download bytes when optimizing previews. ZIP members must each match their source. Original resources are Git-ignored and unchanged; new folders are not automatically published.

If newly copied public files return 404 from an already running development server, restart only this project's verified Astro process and reload. During this import, new video files were absent from the running server's asset index until restart; production builds already served them correctly. Verify actual playback rather than relying only on posters or a successful build.

Research directions now use two columns above 760px and one column at or below 760px. Each card stacks its preview above its text and uses compact 30px-high download buttons. A direction with one paper occupies only the first column; adding its next paper automatically fills the second. Video previews play muted and loop inline when visible, with native controls retained for pause/resume. They start without src and with preload=none; the shared controller attaches src with preload=metadata near the viewport and requests play only when visible. Manual pauses persist across scrolling. These settings are shared in ResearchPaperCard and research-video.ts, not repeated per paper. Original video audio remains in the asset and can be enabled using the controls. Browser-level playback restrictions can still require manual playback.

Edit [research.json](../src/data/research.json): each `directions` entry contains a stable lowercase hyphenated `id`, `title`, `subtitle`, and `papers` array. Array order controls the index, numbered sections and paper cards. IDs must be unique within their direction and remain stable; direction IDs are globally unique. The schema rejects incomplete titles/summaries, unsupported media types and invalid file paths during build. Plain text is escaped, and subtitles display without parentheses.

Each paper has `id`, `title`, `summary`, optional `citation`, optional `preview` (image with `src`/`alt`, or video with `src` and optional `poster`/`captions`), plus independent optional `pdf` and `demo` objects (`src`, optional download `filename`). The user removed the visible media description for all cards; do not add `preview.description` or a caption below the media. Videos retain a title-based accessible name. The `pdf` field renders a Paper button with a document icon; `demo` renders a Demo button with a video/play icon. All asset sources are paths relative to `public/`; recommended directories are `public/research/images/`, `public/research/previews/`, and `public/research/downloads/`. Supply actual files before adding their paths; path validation does not check file existence. Keep short previews separate from full-demo files. MP4/WebM are suitable when encoded for target browsers; narration should have supplied WebVTT captions. Images use contain sizing to preserve diagrams.

The first populated paper is `eeg-fatigue-interaction` under `wireless-without-batteries`. Its current site assets are `preview.mp4`, `poster.jpg`, and `demos.zip` in `public/research/eeg-fatigue-interaction/`; paper.pdf was subsequently withdrawn at the user's request. The preview is an unchanged copy from `resources/display`; ZIP contains the complete `1.mp4`, `2.mp4`, `3.mp4` from that paper's demo directory, without recompression. When replacing a demo set, rebuild the ZIP and verify each entry against its source; never substitute the short preview for the full demo. The title follows the PDF text, and no venue/year was invented. The [initial import record](verification/research-eeg-2026-09-18.md) is historical, not the current PDF publication policy.

[ResearchPaperCard.astro](../src/components/ResearchPaperCard.astro) renders shared cards. Missing Papers have disabled labels, missing Demos have no button, and empty directions show one explicit placeholder. Downloads use same-origin files and Pages base-path support; external hosting and upload forms are not implemented. Do not automatically classify publications or publish new resources. Preview locally at http://127.0.0.1:4321/research/; data examples and verification are in [Research](research.md).

## People maintenance

Edit [people.json](../src/data/people.json): `sections` lists the three groups (pi / phd-students / master-students) in display order; array order inside `members` controls card order. Each member has a stable lowercase-hyphenated `id`, `name`, optional `role` (PI shows it under the name), `photo` as `people/<id>.webp`, `email` (required, rendered as a mailto link), optional `age` (integer; the PI has none by user instruction), and `bio` as an array of paragraphs rendered as escaped plain text (no Markdown/HTML). Age and email appear in the profile dialog and on the static detail pages. The index shows the PI bio inline in the featured block; student cards show photo + name only. Clicking any card opens the shared profile dialog (`PersonDialog.astro`, native `<dialog>` with a dimmed backdrop); the same `/people/{id}/` static pages remain as the no-JS fallback and shareable links, generated automatically from `src/pages/people/[id].astro` — adding a member or bio requires no page or dialog code. The schema rejects duplicate IDs, empty bios, missing photos. Adding a member: drop the original photo under `resources/people/`, extend the mapping in `scripts/prepare-people-photos.mjs`, run `node scripts/prepare-people-photos.mjs` (sharp: EXIF rotation, max 960px edge without enlargement, lossy WebP q82), then add the record. `resources/` originals stay Git-ignored and untouched. Cards use a fixed 4:5 center crop; landscape photos crop the sides, so check faces after switching a photo. The PI bio is kept verbatim per the user's instruction (mixed Chinese/English included).

Link buttons (2026-09-22, MARS-lab style): each member renders an icon-button row under the card (outside the card link) and a labeled button row on the `/people/{id}/` detail page. `email` always yields an Email button; optional `website` and `googleScholar` fields (http(s) URLs only) add Website and Google Scholar buttons that open in a new tab (`rel="noopener noreferrer"`). Clicking an Email button copies the address to the clipboard and confirms in place for two seconds (green fill, check icon, "Copied"); the `mailto:` href remains as the no-JS and copy-failure fallback. Screen-reader labels follow "Email of {name}" / "Personal website of {name}" / "{name} on Google Scholar". Currently only the PI has `website`/`googleScholar`; supply more per member as information arrives — no code change needed. The profile dialog intentionally shows no buttons to keep its fixed-size layout identical for every member. Run `node scripts/check-people.mjs` for both bases after changes (it verifies the index cards, the dialog interaction, all 16 detail pages, the link buttons and the copy behavior). Verification details: [People record](verification/people-2026-09-22.md).

## Contact maintenance

Edit [contact.json](../src/data/contact.json): `introduction` contains the two collaboration paragraphs, `recruitment` is the supplied PhD/Master invitation, and `email` is `guoxz@zju.edu.cn`. The page renders escaped text and a mailto link. Keep the email as a plain address without Markdown backslashes or `mailto:` in the data. No mail is sent by the site; following the link opens the visitor's configured email client. The user authorized this address for Contact; do not substitute the earlier GitHub-related email. Preview at http://127.0.0.1:4321/contact/. No address, map, application criteria or form backend has been supplied or implemented.

## Publications maintenance

Edit [publications.json](../src/data/publications.json) for publication content. The 74 retained records keep stable `id` values p01–p75 excluding p71 and `sourceNumber` values matching the supplied document. The user explicitly deleted p71 (CWSN 2021) on 2026-09-18; do not restore it or renumber other entries automatically. Original records remain recoverable from Git and the historical audit. `originalCitation` preserves retained source text for review and is not displayed as raw HTML. Each entry includes `title`, ordered `authors`, `year`, full `venue`, compact `venueShort`, `type` (`journal`, `conference`, `poster`, `book`), `language`, and `verification` (`verified` or `pending`). Optional fields include volume, issue, pages/articleNumber, DOI, HTTPS publication URL, publishedOnline, and isbnPrint. Current counts: 74 total, 31 conferences, 5 records in 2021, 73 resource links. Only p03/p37 remain internally pending. Dated notes below preserve the earlier state.

The display currently uses verified formal issue/conference years, with year ties ordered by sourceNumber. Online dates remain separate. Keep conference and journal editions as separate records; do not deduplicate by title. On 2026-09-18 the user designated the supplied document as authoritative for p03/p34/p37/p71 and requested removal of Details pending. These four records display normally, including the supplied Chinese title punctuation of p37. `verification` describes historical independent lookup evidence only: it is not rendered, is not a publication status, and does not require the user to supply more evidence before displaying these records. Only change that internal audit flag when evidence supports it; do not invent publication URLs. All original citations remain preserved.

Years and types are generated from the content. Counts next to filter options are totals for the full collection, while the results count reflects active filters. Search matches rendered citation text; words combine with AND, checkbox values within a group with OR, and different filter groups with AND. Mobile filters start collapsed. Author emphasis currently highlights Xiuzhen Guo/郭秀珍 in the entry component and is not a corresponding-author claim. Images, research topics, Featured, award badges, CCF/SCI/IF, and corresponding-author annotations are not invented or imported as verified labels. The audit record preserves those source notes for later review.

Cite (2026-09-22): every publication registered in `src/data/citations/sources.json` (currently 73; p03 is not registered because its publication status is unconfirmed) renders the shared Cite entry next to the Paper link. The exports live at `src/data/citations/{id}.{txt,bib,ris}` with the registry entry supplying the publisher URL and text style (`Cell`, `IEEE`, or `GB/T 7714` for the two Chinese articles), and are statically exported under `/publications/citations/`. To add one, gather the three formats from the paper's DOI (content negotiation) or official venue page, register the id, and all three appear; a missing format file fails the build. The filter search excludes the dialog text, so search still matches card content only. Run `node scripts/check-publication-citations.mjs` after both root/base builds. See [verification](verification/publications-cite-2026-09-22.md).

Later on 2026-09-18, the user supplied the official CCF page for p37 and authorized the website update. Its displayed title is now `关于“射频计算”的思考`, with that page as `url`; both the title and Paper link use it. The collection now has 72 resource links. The original citation remains unchanged; volume 19 and pages 1–4 still come from the supplied document. The historical `pending` flag remains because those fields have not been independently confirmed, not because an official article page is missing. See [Publications](publications.md) for the source and verification record.

Preview at http://127.0.0.1:4321/publications/ after launching start-preview.cmd, or rebuild for production preview. Local changes do not update GitHub Pages until a separately authorized push. See [verification](verification/publications-implementation-2026-09-18.md).

Latest replacement (2026-09-18): the user explicitly replaced p34 with `跨技术通信研究`, Guo Xiuzhen/He Yuan, 2023, 60(1):191–205, DOI `10.7544/issn1000-1239.202110441`. Its complete core citation was confirmed on the journal's official page, so `verification` is now `verified`; p03/p37/p71 remain internal `pending` records. Preserve p34's originalCitation as the original imported text and consult publications.md for the authorized replacement. The collection remains 75 records with 73 resource links; 2023 now contains 10 records and 2021 contains 6.

After visual changes, verify the actual running development preview as well as production screenshots. On 2026-09-18 the long-running Astro server returned an old PublicationCard CSS module even after a browser refresh, while the source and production build were current. Restarting this project's dev server and reloading the page resolved it. If it recurs, stop this project's preview with Ctrl+C and rerun start-preview.cmd; do not stop unrelated Node processes or delete project files. Confirm the browser's computed venue font is Georgia/serif, 20px italic (Chinese: 18px normal), rather than relying on an HTTP 200 response alone. The underlying cache-invalidation trigger has not been isolated.

The three About paragraphs, lab's full name, Zhejiang University affiliation, word-cloud image, and seven News records were supplied by the user and are now integrated. The News wording retains the supplied journal titles and first-person award statement; the Chinese award note was rendered as Category B to keep the site English-only. Remaining hero slogans and the small brand mark are design drafts. Detailed research projects, publications, member details/photos, and final logo are still needed. The teacher's GitHub-related email is not approved public contact information.

## 修改加粗、链接和新闻内容

当前工作区中用户已从 src/pages/index.astro 移除顶部简介的渲染段落，因此 introduction 字段虽保留，但不再显示在首页主标题下方。下方关于 introduction 的加粗能力只适用于重新启用该段落的情况。

打开 [home.json](../src/data/home.json)：

- 介绍正文：about.paragraphs 下的三段字符串。
- 新闻正文：news.items 中每条记录的 text。
- 顶部简介：introduction。

Contact 页的 introduction 各段与 recruitment 段（[contact.json](../src/data/contact.json)）同样支持以下两种标记。

在需要加粗的文字两侧分别加两个星号。例如：

```json
"text": "Our work “**BCGscatter**” on low-power bio-interface was accepted by **Nature Portfolio Flexible Electronics**!"
```

页面显示时星号会转成加粗；删去两侧星号即可取消加粗。仅上述正文位置支持此写法，标题、日期、图片说明等其他字段目前按普通文字处理。标记应成对、放在同一段内；无需填写 HTML 标签。保留 JSON 的双引号与逗号，字符串中如果使用英文直双引号，需要写为 `\"`。

加下划线可点击跳转的链接写法为 `[显示文字](https://网址)`。例如：

```json
"text": "Our work was accepted by [Nature Portfolio Flexible Electronics](https://www.nature.com/natelectron/)!"
```

链接规则：网址必须以 `http://` 或 `https://` 开头，其他形式（含站内相对路径、ftp、javascript 等）一律按普通文字显示，不会变成可点击链接；链接在新标签页打开。链接本身不改变文字粗细和字号；如该文字原本加粗（或希望加粗显示），把两种标记组合使用，`**[文字](网址)**` 与 `[**文字**](网址)` 等效，渲染为绿色下划线粗体。悬停时下划线加粗。

粗体显示由 [global.css](../src/styles/global.css) 中的 strong 规则统一控制，目前为 font-weight: 700。日常选哪些文字加粗、哪些文字挂链接只改内容文件。

当前 localhost:4321 是开发预览，保存JSON后会自动更新。如果改用生产预览，保存后需要重新构建再刷新；在项目根目录的PowerShell中可使用现有本地Node：

```powershell
& '.\.tools\node-v24.21.0-win-x64\node.exe' '.\node_modules\astro\bin\astro.mjs' build
```

若使用 npm run dev 启动的开发预览，则内容保存后自动更新。上述操作只影响本地预览，不上传 GitHub。

## Logo and footer maintenance

The latest minimal logo proposal is implemented and awaiting user acceptance. Edit the W waveform geometry, green (#207b66) stroke and orange (#df8747) signal dot in [Mark.astro](../src/components/Mark.astro). [Brand.astro](../src/components/Brand.astro) combines this SVG with the name from site.json, splitting the words into two lines for WiNet / Lab. There is no auxiliary tagline. The .brand-name rules in [global.css](../src/styles/global.css) control the dark (#202923) wordmark and typography. Both header and footer use the same components; do not create separate copies of the logo. The footer upper row contains only the centered brand. Footer layout lives in [Footer.astro](../src/components/Footer.astro) and .footer-top styles. These are design settings, not routine News/content edits. The user cancelled image generation and API-key troubleshooting; this logo uses no image service. No online logo editor has been implemented.

## Online tool proposal — separate approval required


Intended workflow: an authorized editor signs in, fills out forms, previews a draft, and chooses Publish. A repository commit then triggers a static rebuild and updates the site. Ordinary record editing would not require page-code changes.

Candidate: Decap CMS with a GitHub backend, editing the same structured content used by Astro. GitHub Pages cannot host the necessary OAuth backend. Authentication hosting, repository permissions, maintainers, possible costs, and access from China must be decided before implementation. No CMS vendor or authentication service has been approved.

Proposed forms: site settings, Home, news, research, publications, people, and contact details. Events and Collaborators are outside the current scope. The implementation must define stable IDs, required fields, dates, images/alternative text, drafts, and validation. Implement and test actual collection rendering with those forms, sharing schemas rather than duplicating content rules across pages.

Before automatic publication, obtain full-project acceptance and verify the repository. Form submission must preserve the agreed draft/review/publish distinction. Credentials belong in the approved authentication service, never frontend JSON.

## Handoff

Read AGENTS.md, requirements, and progress first. Preserve unrelated edits; keep implementation, verification, publication and user acceptance distinct. The current website upload was explicitly authorized on 2026-09-18. Later tasks still require their own publication scope; local commits alone are not continuing permission to push. Update the current release record when publishing rather than treating historical task restrictions as the current state.

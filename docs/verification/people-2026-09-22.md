# People 页面实现与验证（2026-09-22）

## 范围

用户于2026-09-22提供 `resources/people/` 资料（16人，每人一图一档），授权本地实现 People 页面，功能参考 MARS Lab People。分组为 PI、Ph.D. Students、Master Students 三类。中文自我介绍译成英文；PI 简介按用户要求原文保留（含中英混排）。仅本地实现与 Git 保存，不 push、不发布。

## 资料与映射

- PI：Xiuzhen Guo（1人）。PhD：Haobo Zhang、Hongyu Wang、Junying Huang、Long Tan、Xiangguang Wang、Yifan Yan、Yu Cao、Zikang Zhang（8人）。Master：Binghe Li、Gaoming Yang、Tianyou Li、Xu Chen、Yaobin Zhu、Zeyang Yang、Zhou Yang（7人）。以文件夹后缀 `_PI/_PhD/_Master` 归组，组内按资料文件夹顺序（字母序）。
- 简介来源：10个docx用 Python 标准库解析 `word/document.xml`（pandoc不可用），6个txt直接读取；`resources/` 原文件只读未动。

## 文本处理记录（可追溯）

- 翻译（中文→英文）：Xu Chen、Yifan Yan、Zhou Yang 三份纯中文简介。Xu Chen 略去"男"（英文简介惯例不注性别）；Yifan Yan 原文为个人品质自述、无研究内容，按原意直译并使用性别中立代词 they，未补造研究内容；Zhou Yang 原文仅爱好一句（羽毛球、CSGO），照此翻译。
- 取英文版：Binghe Li（文档含中英双版，取 English Bio）、Xiangguang Wang（取 English 三段，弃中文重复段）。
- 已是英文直接沿用：其余 8 份及 Gaoming Yang。
- 最小修正：Haobo Zhang 的 "the Winet Group"、Yaobin Zhu 的 "the WiNet Group" 改为 "the WiNet Lab"（站名已由用户确认更换）；Zikang Zhang 结尾 "togethe" 补为 "together"。其余原文未改。
- PI 简介逐字保留，含 "She received my Ph.D." 原句、`NSFC !` 空格感叹号及两处中文括注。
- 未渲染：Binghe Li 文档中的邮箱、GitHub、教育经历、爱好字段（默认不放个人链接/额外字段，待用户决定）。

## 照片处理

- `scripts/prepare-people-photos.mjs`（sharp）：EXIF 方向矫正 → 长边≤960 不放大 → 有损 WebP q82，输出 `public/people/{id}.webp`。16张合计约36MB原图 → 约1.25MB（7–223KB/张）。
- 卡片统一 4:5 `object-fit: cover` 居中裁切。三张横版照片（Binghe Li、Tianyou Li、Xiangguang Wang）经模拟裁切图核对：人脸完整/无切脸问题；Binghe Li 为骑行背影照（不露脸），头部基本完整，建议后续用户提供正装照替换。
- `resources/` 原图未修改、保持 Git 忽略。

## 实现

- `src/data/people.json`：三 sections（pi/phd-students/master-students），成员含 id/name/可选 role/photo/bio 段落数组；PI 附 role "Tenure-track Assistant Professor"（来自其简介原文）。
- `src/lib/people.ts`：zod schema（层级 id 唯一、featured 仅一人、photo 必须 `people/<id>.webp` 本地路径）及 `peoplePhotoPath` base 适配。
- `src/components/PeopleCard.astro`：featured（照片左/文字右，≤560px 堆叠）与 grid 两种形态；照片 lazy/async，alt 为 "Photo of {name}"。
- `src/pages/people.astro`：h1 People + 三个分区；`[section].astro` 排除 people，占位路由不再生成。
- `scripts/check-people.mjs`：两种 base 的整页检查。
- 站点视觉沿用现有令牌（绿色衬线标题、白色背景、卡片网格）。

## 验证

- `astro check`：47 文件 0 错误/0 警告/0 提示。
- 构建：根路径与 `SITE_BASE=/WiNet-Lab/`（.tools/base-dist）均 5 页成功。Git Bash 需 `MSYS2_ENV_CONV_EXCL=SITE_BASE` 防止环境变量路径转换。
- `scripts/check-people.mjs` 两种 base 全部通过：标题、三个分组标题、16 姓名/16 照片 WebP 实际加载于正确 base 下、PI featured 在首且含两处中文括注、15 张 grid 卡、axe WCAG 2 A/AA+2.1 AA 零违规、320/390/760/1440px 无横向溢出、无失败请求；并输出整页截图。
- Playwright 回归：原 17 项中 1 项因仍断言 People 占位文案而失败，按 Research/Publications/Contact 实装先例更新为真实断言（三分组标题+16照片）后，17 项全部通过（5.5s）。
- 截图目视：桌面1440px PI 照片左/文字右、两组网格整齐、照片无变形、无溢出重叠；手机390px PI 正常堆叠、单列可读。见 [desktop](people-2026-09-22-desktop.png)、[mobile](people-2026-09-22-mobile.png)。
- 开发预览 `http://127.0.0.1:4321/people/` 与 PI 照片均 HTTP 200。

## 更新批次（同日 13:45 后，用户更新资料后重建）

用户更新 `resources/people/` 后要求重建。与首版差异：Yifan Yan（换成正式英文研究简介：embodied intelligence 与 AI for wireless computing）、Zhou Yang（换成英文研究简介：wireless sensing 与 radio-frequency computing）、Zikang Zhang（第一人称改为第三人称）三份简介更新；PI 照片更换（文字未变）；Xiangguang Wang 文档删除中文重复段（网站本就只用英文段，无影响）；Zeyang Yang 文字不变。文件夹结构与16人名单不变，无需新增翻译。

处理：people.json 三条 bio 替换为新原文；全部照片经 prepare 脚本重新生成（PI 实际变化，其余源未变）；新 PI 照片 4:5 裁切目视核对人脸完整。复验：astro check 零错误、两种base构建5页、check-people 两种base通过、17项Playwright测试通过、4321预览确认新文案与PI照片200。截图已刷新。本地Git提交（见git log），未push。

## 可点击卡片与个人详情页（同日第二批改版）

用户指出简介直接放在列表页导致各行排布不均，要求改为参考MARS Lab的可点击卡片：列表页仅显示照片+姓名，简介点开成员后才出现（含PI）。实现为纯静态详情页（无JS依赖）：`people/[id].astro` 为16名成员各生成 `/people/{id}/`，含返回链接、分组标签、姓名h1、职称（PI）与全部简介段落；`PeopleCard.astro` 改为链接卡片（照片4:5+姓名，PI卡保留职称行，hover照片微缩放/姓名变绿）；索引页三组统一网格、移除featured布局与内联简介，PI单卡限宽340px；schema 去掉 layout 字段。MARS参考核对：其学生卡同样仅照片+姓名+身份，其PI为内联简介——本站按用户明确要求"每个人"都进详情页，与其不同处已在requirements记录。

复验：astro check 零错误；两种base构建21页（5+16）；check-people.mjs 重写后两种base通过（索引16链接卡片href逐一核对、无简介残留、16详情页标题/h1/照片/简介/返回链接、抽样axe零违规、320–1440px无溢出、无失败请求）；17项Playwright回归通过；4321预览索引与详情页200且内容正确。索引与PI详情页截图目视通过，见[desktop](people-2026-09-22-desktop.png)、[mobile](people-2026-09-22-mobile.png)、[detail](people-2026-09-22-detail-desktop.png)。本地Git提交，未push。

## 第三批改版：PI默认简介 + 悬浮窗展示（同日）

用户两项新要求：1) PI简介恢复在列表页默认展示，PI卡片仍可点击；2) 点击卡片不跳转页面，而是背景变暗并弹出悬浮窗显示自我介绍。实现：`PersonDialog.astro` 复用Cite弹窗的native dialog模式（showModal、Escape/背景点击关闭、焦点圈闭与归还、Ctrl/Cmd+点击保留新标签打开）——列表页含一个共享弹窗，脚本按 data-person-open 取people.json填充照片/分组/姓名/职称/简介（textContent防注入，弹窗照片alt为空装饰性）；卡片保留 `/people/{id}/` href 作为无JS回退与可分享链接，有JS时点击被拦截为弹窗且URL不变。PI分区恢复featured布局：照片左、姓名+职称+简介右（照片与姓名两处均可点击开弹窗），学生组保持纯照片+姓名网格。弹窗桌面为照片+文字双列、手机单列。

复验：astro check零错误；两种base构建21页；check-people.mjs扩展后两种base通过——索引15学生卡+PI内联简介（含中文括注）+17个可点击入口、点击学生卡弹窗打开且URL不变、弹窗内容/照片加载、Escape与背景点击关闭、弹窗打开状态axe零违规、320–1440px无溢外、16个静态详情页作为回退全部有效、无失败请求；17项Playwright回归通过；弹窗与首页截图目视通过（[dialog](people-2026-09-22-dialog-desktop.png)）。4321开发预览确认弹窗标记与详情页回退均200。本地Git提交，未push。

## 弹窗紧凑化与照片自然比例（同日第四批）

用户反馈弹窗空白过多、照片比例奇怪。诊断：760px宽弹窗配200px照片列导致文字列过宽且下方留白，照片被强制4:5裁切不自然，独立的关闭按钮行占用垂直空间。修改 `PersonDialog.astro`：弹窗宽度收窄至620px；照片列190px、照片改为**自然宽高比**（移除4:5裁切与width/height属性，object-fit:contain，max-height限制420px/60dvh）；关闭按钮并入文字列首行右侧（分组标签+姓名同行），去掉独立头部行；收紧间距。手机单列时照片限宽190px。

复验：astro check零错误、两种base构建21页、check-people两种base通过、17项测试通过；竖版3:4（Zeyang Yang）与横版4:3（Tianyou Li）照片弹窗截图目视确认比例自然、布局紧凑、图文高度平衡。4321预览200。本地Git提交，未push。

## 边界与待用户决定

- 未 push；本轮不发布。resources 原始资料不入库。
- 首轮待决项中 Yifan Yan/Zhou Yang 简介内容单薄一项已由用户更新资料解决（2026-09-22更新批次）。
- 仍待用户反馈：Binghe Li 照片为背影照、Binghe Li 的邮箱/GitHub 是否展示、PI 简介中 "She received my Ph.D." 等原文表达是否保留（当前按要求逐字保留）。

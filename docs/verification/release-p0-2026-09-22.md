# P0 媒体优化发布记录（2026-09-22 晚）

用户于本轮明确要求"发送到远程仓库"。实际推送过程与线上核验如下；技术发布不等于整站验收。

## 推送经过

- 用户首次授权推送时 github.com HTTPS 全路由阻断（直连/各 IP/IPv6 超时，本机 7890 代理客户端关闭，扫遍本地端口无可用代理），推送暂缓并留后台监视器。
- 16:09:37 并行会话提交 `129c0bd`（Zikang Zhang 照片更新，公开内容）；16:12 直连一度恢复，该会话将 `dcbce97`（诊断文档）、`1f944f4`（P0 实施）与 `129c0bd` 一并推送 main。监视器 fetch 后按"远程已移动"保护中止，核对为超集快进，无需处理。
- 推送内容审计：3 个提交无 PDF/resources 受限路径；mmprism 新增 demo.mp4 为原本已公开的预览原字节。17 项 Playwright 测试在推送前对含照片提交的树重跑通过。

## 部署与线上核验（2026-09-22 16:1x–16:3x 实测）

- Actions 对 `129c0bd` 构建部署 success（c0f3567、60ca9cb 历史运行亦 success）。
- 五个新媒体文件 Content-Length 与本地逐字节一致：magnetic/cardiac/mechanical/mmprism preview.mp4（3,637,987 / 2,848,671 / 7,190,950 / 3,402,685）与 mmprism demo.mp4（6,273,403）。
- 四篇未录用 PDF 线上仍 404。
- 线上真实浏览器（Playwright）端到端：首段视频立即起播（0s）；同排第二段按设计等待首段完整入缓存后 12s 放行（当时链路约 2.4 Mbps）；首段随后循环不再耗带宽；下方五段在滚动前未加载（无 src）；零页面错误。

## 效果说明

按性能诊断的下限链路 1.1–1.4 Mbps 推算：七段预览最高码率 731 kb/s，任何一段单独播放都留有 ≥35% 余量，不再出现"永远缓冲不完"；同排第二段在慢链路约 25–35s 后从 poster 转为播放。访客手动播放任何一段立即并发不受队列限制。

## 交付状态

发布记录、progress/README 更新随后以 docs 提交推送。本地 4321 预览继续可用。待用户线上查看反馈；GIF 式降级后备与 P1（预览去音轨、People 照片降尺寸、词云有损化）仍待另行确认，未实施。

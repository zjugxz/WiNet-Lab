# 发布记录：全量功能发布（2026-09-22）

## 授权与范围

用户于2026-09-22明确指示"将现在的提交都发布到远程仓库"，授权将本地main全部提交推送 zjugxz/WiNet-Lab。范围：普通快进推送 origin main 并核验既有Pages部署；不公开未录用PDF及resources；本地备份分支不上传。

## 推送前审计

- 待推送16个提交（a9c9ef8…60ca9cb），逐提交diff核对：无 resources/ 文件、无 underwater/eeg/cardiac/mmprism 的 paper.pdf、无受限对象；`git status` 仅剩未跟踪 .zcodeignore（不推送）。
- 复验：astro check 零错误；根路径与 /WiNet-Lab/ 构建21页；17项Playwright测试通过。深度检查脚本（people/research-downloads/media-performance/citations）当日均已在相同代码上通过。
- 远程main经 `git ls-remote` 确认仍为196230f（与origin/main一致），可干净快进。本机7890代理未监听，直连GitHub此刻可用，遂按现状直连推送。

## 推送与部署

- `git push origin main`：196230f..60ca9cb 快进成功（无强推）。
- Actions [35697405893](https://github.com/zjugxz/WiNet-Lab/actions/runs/35697405893) Publish WiNet Lab preview：completed / **success**。

## 线上核验（2026-09-22，直连curl）

- 页面/资源 HTTP 200：`/`、`/research/`、`/publications/`、`/people/`、`/contact/`、`/people/haobo-zhang/`（详情页）、`/research/citations/mmexpert.bib`（Cite）、`/research/mmexpert/paper.pdf`（9,131,408字节，已发表论文）。
- 受限PDF全部404：mmprism-sign-language、eeg-fatigue-interaction、cardiac-monitoring、underwater-visible-light-backscatter 的 paper.pdf 旧地址。
- 媒体优化已生效：首页HTML引用 `_astro/winet-lab-wordcloud.*.webp` 480–2154w 自适应srcset；压缩预览在线（eeg 3,002,563字节 vs 优化前约30.4MB；mmexpert 3,936,390、mmprism 6,273,403 与本地逐字节一致）。
- People页含 `data-person-open` 弹窗标记（16卡片+详情页结构）。

## 本次上线内容（相对上次发布196230f）

Home/Research媒体优化（自适应WebP、预览压缩70%、视口懒加载）、People页全部功能（16人、PI内联简介、固定尺寸悬浮窗含年龄/邮箱、16个无JS详情页）、Research第三方向mmExpert/mmPRISM（含mmExpert Cite）、全部研究一句话简介、首页文案更新与中文解禁。

## 边界

技术发布成功不等于用户整站验收；下一步等待用户查看线上效果并反馈。本记录提交后随同一授权推送。
